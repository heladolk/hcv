// This is a universal Jest mock for all image, audio, and video files. When these
// types are imported using the 'import' syntax, jest will use this mock to replace
// them for tests.

module.exports = "test-file-stub";
